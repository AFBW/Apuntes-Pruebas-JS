'use strict'

// Bucle For
// Bucle es una estructura de control que se repite varias veces
let num = 100;

for (let i = 0; i <= num; i++) {
    console.log('Vamos por el numero: ' + i);
    // debugger;
}
